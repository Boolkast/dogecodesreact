// eslint-disable-next-line
export const styles = theme => ({
  right: {
    position: 'absolute',
    right: 0,
    marginRight: 20,
  },
  modalWrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
  modal: {
    width: '30%',
    minWidth: '300px',
    padding: theme.spacing.unit * 3,
  },
});
